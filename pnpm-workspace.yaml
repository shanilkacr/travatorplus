packages:
  - "apps/*"
  - "packages/*"
# pnpm 11: allowBuilds replaces onlyBuiltDependencies (removed in v11).
# workerd + esbuild are required for wrangler on Cloudflare CI.
allowBuilds:
  "@google/genai": true
  esbuild: true
  protobufjs: true
  sharp: true
  workerd: true
